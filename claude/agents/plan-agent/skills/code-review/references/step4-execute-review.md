# 步骤4：执行审核

## 4.1 越界检查（必须首先执行）

对每个提案的变更文件：
- 检查是否在 `allowed_paths` 范围内
- 检查是否命中 `forbidden_patterns`
- 发现越界立即标记为 `violation`

## 4.2 代码质量检查

详见 `review-checklist.md`

## 4.3 风险分级

| 级别 | 说明 | 处理方式 |
|-----|------|---------|
| 高 | 必须修复才能通过 | 阻塞，要求修复 |
| 中 | 建议修复 | 用户决定是否修复 |
| 低 | 可选修复 | 记录，不阻塞 |
