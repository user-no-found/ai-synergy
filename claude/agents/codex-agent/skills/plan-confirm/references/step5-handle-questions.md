# 步骤5：用户问题处理

如果用户在查看确定方案后提出问题/修改意见：

- 不执行本流程
- 根据修改范围决定：

| 修改范围 | 处理方式 |
|---------|---------|
| 小修改 | 直接修改确定方案，不改 `plan_version` |
| 实质变更（任务拆分/合并、scope调整） | 生成新 `plan_version`，回到 `plan-finalize` |
