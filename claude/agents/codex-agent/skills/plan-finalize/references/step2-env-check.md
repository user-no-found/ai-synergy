# 步骤2：环境确认

## 执行

1. 读取`~/environment.md`获取当前环境清单
2. 对照草案中的`dependencies`需求，逐项检查：
   - 已具备：标记为✓
   - 缺失：标记为✗，记录缺失项

## 缺失处理

如果存在缺失项：
- **阻塞流程**
- 输出缺失清单及安装方式
- 告知如何启动 env-agent：
  ```
  请在 Claude 对话中输入：
  使用 env-agent 安装以下环境：{缺失清单}
  ```
- 等待通知"环境已安装"后重新执行环境确认

## 通过条件

无缺失项时自动进入下一步。
